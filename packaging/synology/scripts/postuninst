#!/bin/sh
# Post-uninstall.
#
# Firm rule: music files and the quarantine folder are never touched. The
# user's data outlives the package. DSM removes /var/packages/Cadenza itself,
# and the application database lives under the Container Manager project's
# config volume, which is not ours to delete.
exit 0
